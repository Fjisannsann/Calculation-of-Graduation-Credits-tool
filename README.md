# Calculation-of-Graduation-Credits-tool

## Web版（GitHub Pages向け）

`/web` に、サーバー不要のブラウザ完結版を実装しています。

- 成績PDFをブラウザで読み取り
- CSVをブラウザ内で生成してダウンロード
- 卒業判定をブラウザ内で実行
- 卒業要件DB・科目属性DBは `web/data/*.json` の静的ファイルで管理

公開時は `web` フォルダを GitHub Pages の公開対象に設定してください。
